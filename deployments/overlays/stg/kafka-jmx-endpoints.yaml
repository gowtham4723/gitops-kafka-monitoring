apiVersion: v1
kind: Endpoints
metadata:
  name: kafka-jmx
  namespace: monitoring
  labels:
    app: kafka-jmx

subsets:
  - addresses:
      - ip: <KAFKA_BROKER_2_IP>
        hostname: lnx-crp-shdsvc-bkr-stg-usea-002

    ports:
      - name: metrics
        port: 7071
        protocol: TCP